package intricateoreprocessing;

import net.minecraft.creativetab.CreativeTabs;
import net.minecraft.init.Blocks;
import net.minecraft.item.Item;
import net.minecraftforge.fml.relauncher.Side;
import net.minecraftforge.fml.relauncher.SideOnly;

public abstract class IntricateOreProcessingCreativeTab {
	public static final CreativeTabs tabIntricateOreProcessing = new CreativeTabs(0, "intricateOreProcessing")
    {
        @SideOnly(Side.CLIENT)
        public Item getTabIconItem()
        {
            return Item.getItemFromBlock(Blocks.iron_ore);
        }
    };
}
